models must cull vegetation, and terrain, so that if there's a big wall, it culls whats behind it (occlusion culling: a model like a wall should occlude vegetation/terrain rendering behind it, not just cull itself)